# --- Web Crawling ---
scrapy>=2.11.0
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=5.1.0

# --- Indexing ---
whoosh>=2.7.4                 # Lightweight local search
elasticsearch>=8.13.0         # Use with Amazon OpenSearch

# --- Task Queue ---
celery[redis,rabbitmq]>=5.3.6 # Task queueing
redis>=5.0.3                  # Redis client (can use AWS ElastiCache)
boto3>=1.34.84                # AWS SDK (used for SQS or cloud services)

# --- Database ---
pymongo>=4.6.3                # MongoDB (if you use Atlas or local)
sqlalchemy>=2.0.29            # SQL ORM
psycopg2-binary>=2.9.9        # PostgreSQL (for AWS RDS)

# --- Cloud Integration (AWS) ---
boto3>=1.34.84                # AWS SDK
botocore>=1.34.84
python-dotenv>=1.0.1          # To manage AWS credentials from .env
s3fs>=2024.3.1                # For working with S3 from Pandas or Scrapy

# --- Monitoring / Fault Tolerance ---
tenacity>=8.2.3               # Retry logic
rich>=13.7.1                  # Pretty logs
tqdm>=4.66.2                  # Progress bars
schedule>=1.2.1               # Lightweight scheduler

# --- Testing ---
pytest>=8.1.1
